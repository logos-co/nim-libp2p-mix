# Nimble-generated, local-only
nimble.develop
nimble.paths
nimbledeps/
nimcache/
# `nimble.lock` is a build artefact regenerated by `make deps` on demand
# (see Makefile, README). Intentionally not committed — see #13.
nimble.lock

# Build artefacts
build/
*.exe
*.dll
*.so
*.dylib
*.a
*.o
examples/mix_ping

# Nix build outputs
result
result-*
